<UserControl x:Class="FactorioModTranslator.Views.GlossaryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Glossary Management" FontSize="18" Foreground="{StaticResource AccentBrush}" Margin="0,0,0,10"/>

        <DataGrid Grid.Row="1" ItemsSource="{Binding Entries}" AutoGenerateColumns="False" 
                  CanUserAddRows="False"
                  Background="{StaticResource SurfaceBrush}" BorderThickness="0" 
                  Foreground="{StaticResource TextBrush}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Source Term" Binding="{Binding SourceTerm}" Width="*"/>
                <DataGridTextColumn Header="Target Term" Binding="{Binding TargetTerm}" Width="*"/>
                <DataGridTemplateColumn Width="80">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="Delete" Command="{Binding DataContext.DeleteCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" 
                                    CommandParameter="{Binding}" Background="#c0392b" FontSize="10" Padding="5,2"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <Border Grid.Row="2" Background="{StaticResource SurfaceBrush}" Padding="15" Margin="0,10,0,0" CornerRadius="4">
            <StackPanel HorizontalAlignment="Left">
                <TextBlock Text="Add New Entry" FontWeight="Bold" Margin="0,0,0,10"/>
                <StackPanel Orientation="Horizontal">
                    <StackPanel Margin="0,0,10,0">
                        <TextBlock Text="Original" FontSize="10" Foreground="{StaticResource MutedTextBrush}"/>
                        <TextBox Text="{Binding NewSourceTerm}" Width="200"/>
                    </StackPanel>
                    <StackPanel Margin="0,0,10,0">
                        <TextBlock Text="Translation" FontSize="10" Foreground="{StaticResource MutedTextBrush}"/>
                        <TextBox Text="{Binding NewTargetTerm}" Width="200"/>
                    </StackPanel>
                    <Button Content="Add" Command="{Binding AddCommand}" Width="80" VerticalAlignment="Bottom"/>
                </StackPanel>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>

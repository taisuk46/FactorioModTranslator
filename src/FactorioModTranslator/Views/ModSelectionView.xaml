<UserControl x:Class="FactorioModTranslator.Views.ModSelectionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:converters="clr-namespace:FactorioModTranslator.Converters"
             xmlns:models="clr-namespace:FactorioModTranslator.Models"
             xmlns:System="clr-namespace:System;assembly=mscorlib"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <converters:NullToVisibilityConverter x:Key="NullToVisibilityConverter"/>
        <converters:TranslationModeConverter x:Key="TranslationModeConverter"/>
        
        <ObjectDataProvider x:Key="TranslationModes" MethodName="GetValues" ObjectType="{x:Type System:Enum}">
            <ObjectDataProvider.MethodParameters>
                <x:Type TypeName="models:TranslationMode"/>
            </ObjectDataProvider.MethodParameters>
        </ObjectDataProvider>
    </UserControl.Resources>
    
    <Grid Margin="20">
        <StackPanel>
            <TextBlock Text="Mod Input" FontSize="18" Foreground="{StaticResource AccentBrush}" Margin="0,0,0,10"/>
            
            <StackPanel Orientation="Horizontal" Margin="0,0,0,20">
                <Button Content="Select Mod Folder" Command="{Binding SelectFolderCommand}" Width="150"/>
                <Button Content="Select Mod ZIP" Command="{Binding SelectZipCommand}" Width="150" Margin="10,0,0,0"/>
            </StackPanel>

            <Border Background="{StaticResource SurfaceBrush}" Padding="15" CornerRadius="4" Visibility="{Binding LoadedMod, Converter={StaticResource NullToVisibilityConverter}}">
                <StackPanel>
                    <TextBlock Text="{Binding LoadedMod.Title}" FontSize="16" FontWeight="Bold"/>
                    <TextBlock Text="{Binding LoadedMod.Version, StringFormat='Version: {0}'}" Foreground="{StaticResource MutedTextBrush}"/>
                    <TextBlock Text="{Binding LoadedMod.Author, StringFormat='Author: {0}'}" Foreground="{StaticResource MutedTextBrush}"/>
                </StackPanel>
            </Border>

            <Grid Margin="0,20,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Column="0">
                    <TextBlock Text="Source Language" Foreground="{StaticResource MutedTextBrush}"/>
                    <ComboBox SelectedValue="{Binding SourceLanguage}" SelectedValuePath="Content" Width="100" HorizontalAlignment="Left">
                        <ComboBoxItem Content="en"/>
                        <ComboBoxItem Content="ja"/>
                    </ComboBox>
                </StackPanel>

                <StackPanel Grid.Column="1">
                    <TextBlock Text="Target Language" Foreground="{StaticResource MutedTextBrush}"/>
                    <ComboBox SelectedValue="{Binding TargetLanguage}" SelectedValuePath="Content" Width="100" HorizontalAlignment="Left">
                        <ComboBoxItem Content="ja"/>
                        <ComboBoxItem Content="en"/>
                    </ComboBox>
                </StackPanel>
            </Grid>

            <StackPanel Margin="0,20,0,0">
                <TextBlock Text="Translation Mode" Foreground="{StaticResource MutedTextBrush}"/>
                <ComboBox ItemsSource="{Binding Source={StaticResource TranslationModes}}" 
                          SelectedItem="{Binding SelectedMode}"
                          Width="200" HorizontalAlignment="Left">
                    <ComboBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Converter={StaticResource TranslationModeConverter}}"/>
                        </DataTemplate>
                    </ComboBox.ItemTemplate>
                </ComboBox>
            </StackPanel>

            <Button Content="Execute Translation" Command="{Binding StartTranslationCommand}" Height="40" Margin="0,30,0,0" FontSize="16" FontWeight="Bold"/>
            
            <ProgressBar Value="{Binding Progress}" Height="10" Margin="0,10,0,0" Foreground="{StaticResource AccentBrush}" Background="#333333" BorderThickness="0"/>
            <TextBlock Text="{Binding StatusMessage}" HorizontalAlignment="Center" Margin="0,5,0,0" Foreground="{StaticResource MutedTextBrush}"/>
        </StackPanel>
    </Grid>
</UserControl>
